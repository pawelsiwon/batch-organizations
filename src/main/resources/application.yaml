spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/organization_info
    password: root
    username: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  batch:
    jdbc:
      initialize-schema: always
      platform: postgresql
